overwrite: true
schema:
  [
    "src/schema.graphql",
    "src/schema.writers.graphql",
    "src/schema.post.graphql",
  ]
documents: null
generates:
  src/generated/schema.ts:
    plugins:
      - "typescript"
